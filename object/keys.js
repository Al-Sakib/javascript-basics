const computer = {
    brand : 'MSI',
    price: 95000,
    processor: 'AMD',
    hdd: '1012',
    monitor: 'Dell'
}

const keys = Object.keys(computer);
console.log(keys);

//To get values of the keys
const values = Object.values(computer);
console.log(values);